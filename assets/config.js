/* config.js — единственное место, где меняются внешние адреса.
   Видео героя лежит НЕ в репозитории: оно платится и раздаётся со стороны
   клиента (Selectel Object Storage + CDN). Пока heroVideo пуст, страница
   играет локальный запасной файл video/hero.webm. */

window.SITE_CONFIG = {
  // Пример: "https://<контейнер>.selcdn.ru/hero.webm" или адрес CDN-ресурса.
  // TODO перед продом (см. site/DEPLOY.md §3): перезалить объект как hero.webm
  // (без пробелов и «!» в имени), выставить Cache-Control:
  // public, max-age=31536000, immutable — сейчас его нет, видео перекачивается
  // каждый визит — и поставить перед хранилищем CDN-ресурс вместо сырого
  // *.selstorage.ru.
  heroVideo: "https://378927d7-40c7-486c-a37f-a3b5c4f454a6.selstorage.ru/kak%20!!!!!!!!!!!!-SE_3.webm",
  // mp4/H.264 для Safari до 16 и старых Android. Оставьте пустым, если нет.
  // Пока пусто — на этих браузерах будет только постер (webm не играет).
  heroVideoMp4: "",
  // Запасной файл в репозитории. Играет, если оба адреса выше пусты или
  // недоступны. Держите его лёгким (сейчас 1,3 МБ).
  heroVideoFallback: "video/hero.webm",
  heroPoster: "img/hero-poster.jpg",

  // Куда уходит заявка со страницы /zayavka.html. Пока пусто — форма
  // проходится целиком и показывает все состояния, но НИЧЕГО НЕ ОТПРАВЛЯЕТ
  // (в консоли предупреждение). TODO перед продом: см. site/DEPLOY.md § 4.
  // Ожидается endpoint, принимающий POST с JSON:
  // { type, city, date, slot, name, contact, about }.
  bookingEndpoint: "",
  // Почта для второго пути: показывается, когда отправка не удалась, и в
  // футере. Заменить на реальную вместе с [ПОЧТА] в разметке.
  email: "[ПОЧТА]"
};
